export enum ECliOptions {
  TimeTravel = 'time-travel',
  Decode = 'decode',
  Find = 'find',
}

export enum ECliFlags {
  Help = '--help',
  ShortHelp = '-h',
  Path = '--path',
  ShortPath = '-p',
  Value = '--value',
  ShortValue = '-v',
  Keys = '--keys',
  ShortKeys = '-k',
  Ip = '--ip',
  ShortIp = '-ip',
  Json = '--json',
  ShortJson = '-j',
  Method = '--method',
  ShortMethod = '-m',
  SaveDecoded = '--save',
  ShortSaveDecoded = '-s',
}
